package frc.robot.subsystems;

import edu.wpi.first.math.Matrix;
import edu.wpi.first.math.Nat;

public class PathSub {
    Matrix mymap;
    public PathSub(){
        this(1646,823);
    }
    public PathSub(int x_size,int y_size){
        mymap = new Matrix(N19(),N823());
    }
}
